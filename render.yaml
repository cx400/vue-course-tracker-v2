services:
  - type: web
    name: vue-course-tracker-v2
    env: node
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && npm install
    startCommand: cd backend && node app.js
    envVars:
      - key: NODE_VERSION
        value: 18
